# 놀란 프로모션 랜딩 — 개발 핸드오프

기준 커밋: `e029d94` · 작성일: 2026-08-13

## 1. 개요

글레이즈(GLAZE) "놀란" 드라마 릴스 콘테스트 프로모션 랜딩. **빌드 도구 없는 단일 `index.html`** 정적 페이지로, CSS/JS 전부 인라인이다. 외부 의존성은 Pretendard 폰트 CDN(jsdelivr) 하나뿐이다.

- 로컬 실행: `index.html`을 브라우저로 열면 끝 (서버 불필요)
- 배포: GitHub Pages, main 브랜치 루트 (`.nojekyll` 포함)

### 저장소 / 배포 URL

| 저장소 | URL | 상태 |
|---|---|---|
| `suuuuunchan/nolan-promotion` | https://suuuuunchan.github.io/nolan-promotion/ | 원본. 현재 최신 |
| `suuuuunchan/nolan-promotion-page` | https://suuuuunchan.github.io/nolan-promotion-page/ | 복제본. 최신이지만 정리 대상 |

⚠️ 같은 코드가 두 저장소에 있다. **하나를 정식으로 정하고 나머지는 아카이브할 것** (이력은 동일, fast-forward 관계). GitHub Pages는 `cache-control: max-age=600`이라 배포 후 **최대 10분간 이전 버전**이 보일 수 있다.

## 2. 뷰포트 전략 (중요)

```html
<meta name="viewport" content="width=375" />
```

**375px 고정 캔버스**를 기기 폭에 맞게 브라우저가 통째로 스케일한다. 모든 좌표가 피그마 375 기준 px 그대로이고, 미디어쿼리가 없다. 이 전제 때문에:

- 히어로 헤드라인은 `<img>`가 아닌 **인라인 SVG**다 — `<img>`는 확대 전 해상도로 래스터화된 뒤 늘어나 뭉개진다.
- 태블릿/데스크톱에서는 페이지가 크게 확대된다(의도된 모바일 전용 설계).
- 반응형 개편 시 이 메타태그부터 손대야 하며, 절대좌표 레이아웃 전체가 영향을 받는다.

## 3. 디자인 소스

피그마: [운영 디자인 / 놀란 프로모션 페이지 (1897:3739)](https://www.figma.com/design/Zwsfv008Icd0ycO8JGBzWd/%EC%9A%B4%EC%98%81-%EB%94%94%EC%9E%90%EC%9D%B8?node-id=1897-3739)

CSS 주석에 피그마 노드 ID를 병기해 뒀다 (예: `히어로 (1897:3739)`, `상품 이미지(1897:3744)`). 좌표 수정 시 피그마 해당 노드와 대조하면 된다.

## 4. 코드 맵 (index.html, 2,467줄)

### CSS (`<style>`, ~43–1300줄)

| 블록 | 위치(≈줄) |
|---|---|
| 앱바 | 43 |
| 히어로 | 86 |
| 스크롤 어피어(blur-in) `.reveal` | 203 |
| 당첨 상품 | 237 |
| 참여 방법(아코디언 + 데모 씬 1~3) | 328 |
| 심사 기준(가로 캐러셀) | 784 |
| 심사 발표 | 863 |
| 참여 플로우(모달 3단계) | 912 |
| 이벤트 안내(약관) | 1283 |

### JS (`<script>`, ~1697–2467줄)

| 블록 | 위치(≈줄) | 역할 |
|---|---|---|
| 자동 투어 | 1699 | 참여 방법 카드 1→2→3 자동 진행, 유저 조작 시 즉시 해제 |
| 아코디언 | 1712 | 한 번에 하나만 열림 |
| 스크롤 어피어 IO | 1729 | `.reveal`에 `.in` 부여 (1회성). `.steps`는 타이틀/카드묶음(`.step-list`) 분리 관찰 |
| 심사 기준 캐러셀 | 1756 | 스크롤 위치 연동 포커스 blur |
| 컨페티 | 1822 | 응모 완료 화면 |
| 참여 플로우 | 1932 | 링크 입력 → 파일 첨부 → 완료 (아래 6장 참조) |
| 히어로 영상 재생 보장 | 2007 | 자동재생 차단 시 첫 인터랙션에서 재생 |
| 카운트다운 | 2016 | `TIMER_DEADLINE` 상수 참조 |
| 앱바 로고 페이드 | 2043 | 헤드라인과 겹치면 숨김 |
| 데모 공통 게이트 `attachCardDemo` | 2135 | **열림+가시성(패널 60% 이상)** 둘 다 참일 때만 재생. 이탈 시 정지, 복귀 시 처음부터 |
| 데모 1/2/3 | 2241 / 2316 / 2427 | 타이밍은 `W1`/`W2` 배열(ms). 사이클 9.2s / 11.4s / 5.4s |

## 5. 인터랙션 명세 요약

- **히어로 영상**: 루프 없이 1회 재생 후 마지막 프레임 홀드 (`hero_video_hold.mp4`).
- **스크롤 어피어**: 그룹 상단이 뷰포트 하단 30% 지점을 넘으면 그룹 내 `.reveal`이 `--d` 스태거로 blur-in. 새로고침 스크롤 복원 시 이미 지나친 그룹은 즉시 표시.
- **참여 방법**: 1번 카드가 **마크업에서 기본 열림**(`class="… open"` + `aria-expanded="true"`). 데모는 화면에 보일 때만 재생. 한 사이클 완주 시 다음 카드로 자동 진행(자동 투어), 3번에서 종료.
- **모션 접근성**: `prefers-reduced-motion: reduce`에서 `.reveal` 애니메이션 비활성.

## 6. 백엔드/앱 연동 필요 지점 (현재 전부 프런트 데모)

**네트워크 요청이 단 하나도 없다.** 실서비스 전 반드시 연결해야 할 것들:

1. **카운트다운 종료 시각** — `TIMER_DEADLINE`(≈2019줄). 현재 `null`이라 진입 시점부터 3시간 22분을 세는 데모 동작. 실제 마감 시각(예: `"2026-08-31T23:59:59+09:00"`)을 넣거나 서버 시간으로 대체할 것.
2. **참여 플로우 제출** — 릴스 링크(`.flow-input`)와 원본 파일(`input[type=file]`, `video/*,image/*`)이 **어디로도 전송되지 않는다**. 파일은 `createObjectURL` 로컬 미리보기만. 확인 모달 "네" 클릭 시 완료 화면으로 넘어갈 뿐이다. 업로드 API + 유효성 검사(링크 형식, 파일 용량 제한) 필요.
3. **앱바 X 버튼**(`.appbar .close`) — **클릭 핸들러가 없다** (플로우 모달 안의 X는 동작함). 웹뷰 닫기 브릿지 연결 필요.
4. **당첨 카운트** — "0/1", "0/3" 하드코딩. 실데이터라면 API 연동.
5. **메타/공유 태그 없음** — og:title/og:image/description, favicon 미설정. 프로모션 특성상 공유가 많을 테니 필수.

## 7. 에셋 인벤토리 (assets/, 총 20MB)

### 사용 중 (27개)

히어로: `hero_video_hold.mp4`(4.4MB) · 타이머 `timer-pill.svg` · 로고 `logo-1~4.svg` · 앱바 `xmark.svg`
당첨 상품: `prize-hero.png`(@2x) · `prize-sugar1/2.png`(@3x)
참여 방법 데모: `story-bg/man/woman.png`, `camera-cursor.svg`, `s2-*.png`(5개), `hand.svg`
심사 기준: `crit-1~4.png` · 심사 발표: `announce-bg.png` · 플로우: `chevron-left.svg`

### 미사용 (10개, 8.9MB) — 삭제 권장

| 파일 | 크기 | 비고 |
|---|---|---|
| `hero_video.mp4` | 4.4MB | 홀드 버전으로 대체됨 |
| `raw-1.png` / `raw-2.png` | 2.1MB | 원본 소스 |
| `hf_20260812_…mp4` | 1.7MB | 생성 원본 |
| `hero.png` | 1.0MB | 미사용 (영상 poster 후보로 쓸 수는 있음) |
| `hero-headline-1a/1b/2.svg` | 15KB | 인라인 SVG 전환으로 대체됨 |
| `icon-2user-a/b.svg` | 3KB | 미사용 |

삭제해도 git 이력에 남아 복구 가능.

## 8. 확인 필요 (디자인 vs 코드 카피 불일치)

참여 방법 카드 설명이 피그마 최신 텍스트와 다르다. 의도된 수정인지 확인 필요:

| 카드 | 코드 | 피그마(1897:3827/3801/3809) |
|---|---|---|
| 1 | "…선택지를 **캡쳐**해주세요." | "…선택지를 **골라**주세요." |
| 2 | "…설문캡션과 함께 **릴스를** 올려주세요." | "…설문캡션과 함께 올려주세요." |
| 3 | "…릴스 링크를 **등록**해주세요. 승인 후…" | "…릴스 링크를 **올려**주세요. 승인이 완료되면…" |

부상 카피 "슈가 5만~천개"도 표기 확인 권장.

## 9. 알려진 제약 / 주의

- **iOS safe-area 미처리** — 홈 인디케이터 영역 `env(safe-area-inset-*)` 대응 없음. 페이지 끝이 약관이라 실害는 적지만 앱 웹뷰 삽입 시 확인.
- **데모 타이밍 수정법** — 카드 1·2는 `W1`/`W2` 배열(ms)이 진행바 세그먼트 비율과 모션 대기를 겸한다. 배열만 고치면 진행바도 함께 맞춰진다. 카드 3은 개별 `demoWait()` 값.
- **아코디언 열림/닫힘**은 `grid-template-rows 0fr↔1fr` 트랜지션. `height: auto` 트릭 불필요.
- **`.reveal`과 `translateX(-50%)` 충돌 금지** — 어피어는 `translate` 속성, 센터링은 `left: calc(50% - …)` 방식을 쓴다. 기존 주석 참조.
- 콘솔 오류 0건, 404 에셋 0건 (2026-08-13 전수 확인).

## 10. QA 체크리스트

- [ ] 히어로: 영상 1회 재생 후 홀드, 타이머 카운트다운, X/로고 위치
- [ ] 스크롤: 각 섹션 blur-in이 화면 안에서 보이는지
- [ ] 참여 방법: 1번 기본 열림 → 보일 때만 데모 재생 → 자동 투어 1→2→3 → 수동 클릭 시 투어 중단
- [ ] 화면 이탈 시 데모 정지, 복귀 시 처음부터 재생
- [ ] 플로우: 링크 입력 → 다음 → 파일 첨부(미리보기/재선택) → 참여하기 → 확인 모달 → 완료 컨페티 → 확인으로 닫기
- [ ] 뒤로가기(←)/X 버튼 각 단계 동작
- [ ] iPhone SE(소형)·Pro Max(대형) 실기기 스케일 확인
- [ ] `prefers-reduced-motion` 켠 상태에서 콘텐츠 정상 노출
